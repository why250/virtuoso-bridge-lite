# Execute supplied SKILL read-only

```text
Work in D:\Users\Administrator\Documents\GitHub\virtuoso-bridge-lite and
read AGENTS.md first.

Use the already deployed Virtuoso bridge.  First run the non-mutating probe
VirtuosoClient.from_env().execute_skill("1+1").  Stop and report the error if
it fails.

Execute the following SKILL in the current Virtuoso session:

<SKILL code>

Return its result, errors, warnings, and relevant CIW output.  Do not modify,
save, close, or create any design objects beyond what the supplied SKILL itself
explicitly requests.
```
