# Run a remote Spectre simulation

```text
Work in D:\Users\Administrator\Documents\GitHub\virtuoso-bridge-lite and
read AGENTS.md first.

Use the configured remote Spectre environment for this task.  First check the
netlist, model include paths, simulator availability, and license state.  If a
required input is missing, report it before starting a simulation.

Simulation goal: <analysis, corners, stimuli, measurements, and acceptance criteria>
Inputs: <netlist path or design/testbench details>

Store generated local scripts and result summaries under workspace.  Preserve
the original design and netlist unless this request explicitly authorizes an
edit.  After the run, report the command/setup used, run status, key measured
values, and the location of outputs.
```
