# Inspect or operate Maestro/ADE

```text
Work in D:\Users\Administrator\Documents\GitHub\virtuoso-bridge-lite and
read AGENTS.md first.

Use the already deployed Virtuoso bridge.  First run the non-mutating 1+1
probe.  If it fails, stop without changing anything.

Maestro/ADE target: <session, testbench, history, or current focused session>
Goal: <inspect setup / create setup / run simulations / export results>

First perform a read-only inspection of the active Maestro/ADE session and
report the session, tests, corners, variables, analyses, and result directory.
Do not overwrite an existing setup or launch simulations until the target is
unambiguous.  After an authorized change or run, summarize the exact session
state and output locations.
```
